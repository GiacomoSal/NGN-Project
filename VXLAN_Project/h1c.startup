# ===== h1c.startup (Tenant C - Lato s1) =====
#!/bin/bash
ip addr add 192.168.30.1/24 dev eth0
ip link set eth0 up
ip route add default via 192.168.30.254
echo "h1c (Tenant C) configuration completed"
ip addr show eth0