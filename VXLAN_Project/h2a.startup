# ===== h2a.startup (Tenant A - Lato s2) =====
#!/bin/bash
ip addr add 192.168.10.2/24 dev eth0
ip link set eth0 up
ip route add default via 192.168.10.254
echo "h2a (Tenant A) configuration completed"
ip addr show eth0