#!/bin/bash
ip addr add 192.168.100.1/24 dev eth0
ip link set eth0 up

# IMPORTANTE: Aggiungere default gateway verso s1
ip route add default via 192.168.100.254

echo "h1 configuration completed"
ip addr show eth0
echo "Routes:"
ip route show